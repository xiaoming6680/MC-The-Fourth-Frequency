# 进度日志

## 2026-07-21：固定频段退场实施完成

- **状态：** complete
- 已删除玩家可见的固定电台、三频校准、自动/手动轮播和全局调频器；四页终端与六项工具保持不变。
- 绑定改为设置住处或自然取得铁任一项；取得铁仍启动标志场景，绑定后由该场景揭示无法归类的信号层。
- 调谐只在“工具 → 信号”且附近存在真实候选时开放；候选峰值稳定派生，误差 ≤2 连续 20 Tick 后由服务端复核完成。
- 工具快照升级到 v2；主快照保持 v5。旧校准位图、自动调频字段、日志 band 线号和废弃动作编号继续兼容读取，但不再驱动行为。
- 记录页聚合全部日志并支持一次性全部已读；候选不再按四个频段分组。中英文资源、README、架构、世界观、测试与验收文档已同步。
- 首轮客户端失败追踪到测试夹具残留固定值 55，并非生产锁定音缺失；夹具改用真实候选峰值后完整 mainline 通过。

### 最终验证

| 测试 | 结果 | 说明 |
|---|---|---|
| `compileJava compileClientJava compileGametestJava compileTestJava` | 通过 | 三组生产/客户端/GameTest/单元测试源码均可编译 |
| 完整 JUnit | 通过 | 91/91 |
| 全量服务端 `runGameTest` | 通过 | 38/38 |
| `runClientGameTest -PtffClientTestSuite=mainline` | 通过 | `BUILD SUCCESSFUL`，1 分 53 秒；近场接收器、碎片、结局和保存重开通过 |
| `assemble` 与资源校验 | 通过 | 23 秒；中英文各 610 键且集合一致；发布 JAR 10,346,223 字节，SHA-256 `06504F6A16BB1A3D1109474ACE265F08DA5F1C51964CC7FD84B4B827443810C0` |

- 技能自带完成检查器因本机执行策略、脚本编码和 shell 缺失无法运行；等价人工核对结果为 0 个未勾选项、0 个非 complete 状态、11 个 complete 阶段。未改动当前指向其他任务的 `.planning/.active_plan`。

## 2026-07-21：固定频段退场实施规划

- **状态：** complete
- 用户实机测试后认为原频段设计已经冗余，并追问具体实施方式；本轮先产出代码级规格，不修改业务代码。
- 已完整读取文件规划技能、当前任务计划、发现、进度和旧实施规格。
- 已确认旧规格第 5 节“调频弱化”的自动轮播方案被玩家试玩结论取代；新方向是删除固定频段/校准/轮播，只保留近场异常调谐。
- 已新增阶段 9–11，开始盘点调用、迁移和测试依赖。
- 已完成 `frequency_retirement_spec.md`，锁定主线替代、近场接收器状态机、网络 v2、schema v5 兼容和四批实施顺序。
- 用户已授权规格确立后自动实施；阶段 9 完成，阶段 10 开始。

## 2026-07-21：当前 MOD 流程复核

- **状态：** complete
- 用户要求梳理 MOD 当前流程；本轮只读复核生产源码与配置，不修改业务代码。
- 已恢复并读取现有专项计划、发现与进度，确认前七阶段已完成；新增阶段 8 记录本轮流程复核。
- 已完成入口、事件订阅、终端状态机、生存里程碑、异象/校正体、四碎片文件、要塞祭坛、三结局、客户端首次启动与持久化边界的源码追踪。
- 首次组合文件枚举命令因 `rg` 未找到 `AGENTS.md` 返回退出码 1，但已正常取得后续文件列表；确认项目内没有 `AGENTS.md`，未重复该失败组合命令。
- 已确认 README 与当前四页终端/schema v5 存在文档漂移，并定位终止钉未接入、旧设施未分配、多人任务字段未补齐、祭坛前置仅为软引导等当前遗留。
- 已完成玩家体验流程图、服务端状态边界和遗留风险整理；本轮没有修改 MOD 业务代码，也没有运行构建或测试。

## 2026-07-20

### 阶段 3：终端 UI 基础重制
- **状态：** complete
- 已完整读取 `implementation_spec.md`、`task_plan.md`、`findings.md`、`progress.md` 与文件规划技能说明，确认沿用已锁定规格实施。
- 文件规划恢复脚本第一次运行失败：系统 `python` 命令是 Microsoft Store 别名；未据此修改源码，后续改用已配置运行时或人工核对工作区。
- 下一步：检查 Git 工作区、构建入口、终端 UI 与相关测试的当前实现，然后开始四页导航和基础布局修改。
- 已完成首次工作区核对：没有 `AGENTS.md`；仓库没有提交，所有项目文件均未跟踪；不会使用 Git 清理或覆盖现有内容。
- 已确认旧布局仍是 SIGNAL/FILES 双标签与三列两行文件卡片，`TerminalScreen` 约 1289 行，右侧硬件区域可原样保留。
- 第一次基线 Gradle 调用因工具只给了 1 秒超时而被终止，尚未得到测试结果；下一次改用足够超时，不把该结果记作项目失败。
- 基线验证通过：`compileJava`、`compileClientJava` 与终端相关定向测试成功，JUnit 80/80；这将作为阶段 3 改动前基线。
- 已新增 `TerminalPage`、`TerminalTool` 并把 `TerminalUiLayout`/布局测试改成四标签、主页、固定六宫格、记录区、文件 30/70 分栏和自动/手动显示区域。
- 新布局首次编译在旧 `TerminalScreen` 的 17 处旧常量引用上失败；这是布局与屏幕分步迁移产生的已定位断点，下一步直接完成屏幕迁移后重测。
- 四页屏幕首次收口编译只剩 2 处枚举类型错误：工具可见性误用了 `SignalBand`；已定位为机械类型问题。
- 已加入中英文四页、主页、六工具、文件空白提示和自动/手动调频文案；两份 JSON 均可解析，中文资源中已找不到规定禁止的旧说法。
- 已完成 `TerminalScreen` 四页迁移：主页显示单一任务/进度、两个快捷工具和最近事件；工具页使用固定 3x2 插槽并在原页打开详情；记录页不再按频率过滤；文件页使用独立列表/正文滚动且进入时无选中项。
- 文件页现在把四个碎片文件也放入左栏；加密日记未齐时右侧显示碎片状态，齐全未解锁时显示可用密码键盘，解锁后在同一区域显示完整正文。
- 自动调频不会在初次 `calibrate` 任务完成前启用；完成后滑块会在已知频率间自动移动，玩家仍可切回手动，干扰层没有改变任何交互边界。
- 已更新静态契约、布局单测和 `M0ClientGameTest` 的四页/文件列表断言；`compileGametestJava` 与 JUnit 83/83 通过。
- `mainline` 客户端 GameTest 通过，耗时约 1 分 55 秒；新增主页、工具、记录和空文件页截图均生成。
- 视觉复查发现记录页对 `fragment_marker_*` 显示翻译键；正在补齐这类旧内部事件到简单中文的统一映射，修复后会重跑相关验证。
- 已补齐候选信号、信号标记、失去记忆的行动和共享文件事件映射；最终记录页截图只显示简单中文。
- 最终 `mainline` 客户端 GameTest 再次通过，耗时约 1 分 54 秒；阶段 3 完成。
- 本阶段修改：新增 `TerminalPage.java`、`TerminalTool.java`；重制 `TerminalUiLayout.java`、`TerminalScreen.java`、`TerminalSnapshot.java`；更新中英文资源、布局/资源契约测试和 `M0ClientGameTest.java`。
- 下一步是阶段 4：新增独立工具快照 v1，并接入住处、矿物、传送门、当前天气、信号和要塞的服务端真实数据；终端快照 v5 与导航包 v4 保持不变。

### 阶段 1：现状审计与边界冻结
- **状态：** complete
- 已读取文件规划技能的完整说明。
- 已确认根目录存在旧的 task_plan.md、findings.md 和 progress.md，决定不覆盖历史文件。
- 已检查 Git 状态：仓库没有提交，项目文件整体处于未跟踪状态；本轮不清理、不提交。
- 已建立独立计划目录 `.planning/terminal_survival_rework/`。
- 已初步定位终端 UI、布局、快照、控制、导航、文件、身体生成、异象和校正体源码。
- 已确认导航当前仅覆盖三种矿物和结构碎片，且剧情绑定后会主动停用矿物寻找；这与“便利工具贯穿生存主线”的新目标冲突。
- 已确认四页终端可优先做成客户端页面状态，继续映射到现有 SIGNAL/FILES 两种服务端 mode，减少协议改动面。
- 已确认文件重制需要替换三列网格与全屏详情，并为左侧列表和右侧正文建立独立滚动状态；现有进入 FILES 时清空选择的行为可以保留。
- 已确认旧测试会主动拒绝任务提示并要求三列文件网格，必须随新需求同步改写。
- 已列出身体生成对 Boss、异象、世界变化、行为采样、终端外观、文件和调试链的主要依赖，后续不能用简单删除服务注册替代迁移。
- 已确认住处、传送门位置、末影之眼轨迹和要塞估算目前没有可直接显示的数据；天气只能先可靠显示当前情况。
- 已确认现有校正体只在后期出现，并在没有工作目标时直接追击终端携带者；新机制需要重做目标与敌对条件，而不是单纯提前生成。
- 已确认 12 个文件 ID 可以全部保留，通过短标题、正文重写和旧状态迁移完成叙事替换。
- 已完成调用关系与测试契约审计。

### 阶段 2：锁定整合规格
- **状态：** complete
- 新增 `implementation_spec.md`，固定四页布局、APP 网格、文件 30/70 分栏、真实工具数据、自动调频、任务顺序、存档迁移、校正体边界、要塞终局和验证要求。
- 决定保留终端快照 v5 与导航包 v4 的字段形状，新增独立工具快照，避免把所有 APP 数据塞入剧情快照。
- 决定保留现有 12 个文件 ID、历史字段和结局字符串，通过 schema v5 迁移增加新节点。
- 下一步：从终端 UI 基础重制开始实施，先完成界面和定向测试，再接服务端工具数据。

## 测试结果
| 测试 | 结果 | 说明 |
|---|---|---|
| 尚未运行 | — | 当前处于规划和只读审计阶段 |
| 阶段 3 改动前基线：`compileJava compileClientJava` + 终端定向单测 | 通过 | JUnit 80/80，`BUILD SUCCESSFUL` |
| 阶段 3 编译、GameTest 编译与单元测试 | 通过 | `compileClientJava`、`compileGametestJava` 成功；JUnit 83/83 |
| 阶段 3 `mainline` 客户端 GameTest | 通过 | `BUILD SUCCESSFUL`，约 1 分 55 秒；视觉复查另发现记录文案缺口，正在修复 |
| 阶段 3 最终 `mainline` 客户端 GameTest | 通过 | 修复记录文案后 `BUILD SUCCESSFUL`，约 1 分 54 秒；截图复查通过 |

## 错误日志
| 错误 | 尝试次数 | 解决方案 |
|---|---:|---|
| 初次读取持久化 schema 使用了错误目录 | 1 | 通过文件列表定位到 `persistence/PersistenceSchema.java` 后成功读取 |
| 文件规划恢复脚本调用到 Windows Store 的 Python 别名 | 1 | 不重复使用该别名；改用工作区依赖提供的 Python，或以 Git 状态和规划文件进行人工恢复核对 |
| 首次更新阶段状态时补丁上下文漏写列表前缀 | 1 | 使用包含 `- **状态：**` 的精确上下文重新应用补丁 |
| 首次 Gradle 基线验证仅给了 1 秒超时 | 1 | 不重复短超时；改用足够的 120 秒运行同一组编译与定向测试 |
| PowerShell 中把通配路径直接传给 `rg` | 2 | 后续只把目录传给 `rg`，并使用 `-g '*.java'` 过滤；不再向路径参数写通配符 |
| 新布局契约落地后旧 `TerminalScreen` 仍引用已删除的双页/网格常量 | 1 | 立即迁移 `TerminalScreen` 到新布局；不添加会掩盖旧 UI 的兼容别名 |
| 工具可见性判断把 `SignalBand` 传给需要 `TerminalControlPolicy.Band` 的方法 | 1 | 改用正确的 `TerminalControlPolicy.Band` 常量 |
| 英文文案批量补丁假定了旧字符串内容 | 1 | 已读取实际键值，下一次按稳定键和精确旧值替换 |
| 当前 PowerShell 的 `ConvertFrom-Json` 不支持 `-AsHashtable` | 1 | 改用 `PSObject.Properties` 遍历翻译键 |

### 阶段 4：便利工具与弱化调频
- **状态：** complete
- 新增独立 `TerminalToolSnapshotPayload` v1，并在服务端接入六个工具的持久数据、操作、枚举校验和单一罗盘占用。
- 终端快照 v5 和导航包 v4 保持原有数据形状；导航 v4 仅使用旧协议的目标类型编号扩展实际指向。
- 服务端保存自动/手动状态，第三个普通频段首次校准后默认自动；自动时滑块继续显示但服务端拒绝拖动。
- 新增真实维度到达记录与末影之眼服务端样本入口；要塞对客户端只发送方向和距离范围。
- 编译与单元测试最终通过：`compileJava`、`compileClientJava`、`compileGametestJava`，JUnit 86/86。
- 全量服务端 GameTest 最终 37/37 通过。
- `mainline` 客户端 GameTest 最终通过，耗时约 1 分 55 秒；新增住处、天气、信号、矿物、传送门和要塞工具截图，视觉复查通过。
- 方向测试曾暴露导航包每 4 Tick 同步而新场景只等待 2 Tick；已按既有同步节拍修正测试，没有更改导航 v4。
- 客户端视觉测试世界不生成要塞，因此要塞页使用明确的服务器数据夹具验证布局；生产入口仍仅在原版末影之眼真正找到结构时记样本。
- 下一步进入阶段 5：以原版生存节点替换旧身体进度的新触发，并重做校正体目标与敌对规则。

## 阶段 4 测试结果
| 测试 | 结果 | 说明 |
|---|---|---|
| `compileJava compileClientJava compileGametestJava test` | 通过 | JUnit 86/86 |
| 全量服务端 `runGameTest` | 通过 | 37/37 |
| `runClientGameTest -PtffClientTestSuite=mainline` | 通过 | `BUILD SUCCESSFUL`，约 1 分 55 秒，新增工具页截图复查通过 |

## 2026-07-21

### 阶段 6：要塞祭坛与三结局
- **状态：** complete
- 已重新完整读取实施规格、计划、发现和进度文档，并核对工作区：仓库仍无提交、项目文件整体未跟踪，没有 `AGENTS.md`，未清理或回退任何现有改动。
- 文件规划恢复脚本首次仍命中 Windows Store `python` 别名；改用工作区依赖中的 Python 后成功执行，没有未同步上下文。
- 已完成旧终局调用图审计：当前旧服务仍包含跨维度追逐、删除玩家终端、吸收环境方块和非祭坛生成入口；阶段 6 必须将这些运行时行为收束到真实要塞传送门房内。
- 已锁定实现方式：服务端验证真实要塞结构和 12 个末地门框；最后一只眼或明确终端祭坛交互才能开始；四个真实祭坛装置放在传送门内部；最终实体只在祭坛附近活动，不夺取物品、不追到基地、不改造玩家方块。
- 第一轮 `compileJava compileClientJava` 只发现一处 1.21.11 API 类型差异：`RESPAWN_ANCHOR_DEPLETE` 需要从注册表引用取 `.value()`；已修正，不重复原错调用。
- 第一次全量服务端 GameTest 在游戏注册阶段暴露了方块属性问题：普通 `Block` 复制重生锭时，原版光照回调会读取本方块没有的 `charges`。已改用黑曜石属性并保留独立祭坛纹理；本次失败发生在测试注册前，还不是 GameTest 用例失败。
- 第二次全量服务端 GameTest 实际运行 38 项，37 项通过；唯一失败是新夹具漏设三个普通频段校准，任务因此正确停在 `calibrate` 而不是 `find_portal_room`。已补齐夹具的服务端校准状态，不改生产任务选择逻辑。
- 第三次全量服务端 GameTest 仍为 37/38；同一新夹具还漏了 `EYE_SAMPLE_COUNT=2`，任务正确停在 `record_eye`。已把真实流程必需的两次样本一并写入夹具；不降低生产校验。
- 第四次全量服务端 GameTest 中新的阶段 6 用例本身通过，但它留下了世界共享的成功结局，使 5 个并行旧用例看到“异象已停止”的结局状态后失败。已在结局断言完成的同一 Tick 内清除固定测试夹具；不改生产结局持久化。
- 已补齐“校正体最后阻拦”：祭坛装置成为真实校正目标；校正体只拆除目标装置，不在要塞周围放置伤痕或支架。
- 补入校正目标后的首轮服务端回归暴露成功结局会覆盖其他玩家已获得的终端能力；已改为只追加 `weak_resonance`，并增加既有能力保留断言。最终完整服务端 GameTest 38/38 通过。
- 首次 mainline 客户端回归在进入阶段 6 前因跨维度夹具残留坠落距离而死亡，祭坛代码尚未执行；已在测试传送边界重置生命、速度和坠落距离。第二次完整 mainline 通过，约 1 分 55 秒，并通过结局存档重开。
- 已检查新增截图 `0061_r-terminal-altar-protect-task.png`、`0062_r-stronghold-altar-active.png` 和结局完成画面：任务文字直接，四个固定装置是真实渲染方块，最终实体与 Boss 条可见，结局后任务为“故事结束”。
- 已复查双语结局记录文案，移除旧“终端被夺走”叙述，保留原有翻译键、文件 ID 与结局字符串。
- 终端快照 v5、导航 v4、工具快照 v1 的常量与数据形状未改；没有新增网络包。

## 阶段 6 本次改动验证结果
| 测试 | 结果 | 说明 |
|---|---|---|
| `compileJava compileClientJava compileGametestJava test` | 通过 | 三组编译成功；JUnit 88/88，含新增要塞祭坛生产入口静态契约 |
| 全量服务端 `runGameTest` | 通过 | 38/38；覆盖真实门框状态、旧进度不触发、三结局、装置目标、安全边界、记录和固定调试枚举 |
| `runClientGameTest -PtffClientTestSuite=mainline` | 通过 | `BUILD SUCCESSFUL`，约 1 分 55 秒；祭坛任务、真实装置/实体画面、成功结局和存档重开通过 |
| 玩家文案禁止词与协议常量核对 | 通过 | 玩家资源未发现规定禁止说法；终端 v5、导航 v4、工具快照 v1 保持不变 |

## 阶段 6 验证中间问题与项目原有警告
- 本次改动引起的中间失败均已修复：声音注册表引用、祭坛方块属性、两处任务夹具前置、并行结局隔离、成功结局能力覆盖和客户端坠落距离。
- 最终成功构建仍会记录项目原有的 Realms/账号离线鉴权、缺失 `build/resources/client`、`Thumbs.db` 资源路径、各向异性过滤选项以及部分过时 API 警告；这些均未阻挡测试。
- 客户端测试中的祭坛场景是视觉夹具，只用于检查真实方块、实体和终端画面；生产入口对真实要塞结构和门框的服务端校验由 `FinalConfrontationService` 实际执行。
- 仓库仍没有 Git 提交，整个项目仍显示为未跟踪；按用户要求没有清理、覆盖、提交或推送。

### 阶段 5：生存主线与校正事件
- **状态：** complete
- 新增服务端生存里程碑和检测服务，主页任务已改为住处、铁、下界准备、下界往返、末影之眼和要塞。
- 删除旧计时身体进度的新增写入和最终敌人自动唤醒；保留旧字段读取和可控迁移。
- 新增确定性标志事件、工具暂停/自然恢复和完整记录，没有删除物品、住处或其他已保存位置。
- 校正体改为工作导向：不工作时不追人，不挖路，只在攻击、持续挡路或强行操作后暂时敌对。
- GameTest 并行执行暴露了世界只保存一个痕迹的多人缺陷；已改为有界多痕迹与所属玩家记录。
- 终端快照 v5、导航 v4、工具快照 v1 的形状均未改变。
- 新增“做好进入下界的准备”主页截图和标志事件记录页截图；视觉复查通过。
- 下一步是阶段 6：要塞祭坛与三结局；本轮没有开始实施该阶段。

## 阶段 5 本次改动验证结果
| 测试 | 结果 | 说明 |
|---|---|---|
| `compileJava compileClientJava compileGametestJava test` | 通过 | 最终 JUnit 87/87，三组编译全部成功 |
| 全量服务端 `runGameTest` | 通过 | 最终 38/38，含旧进度迁移、1000 不自动唤醒、速通补偿和可恢复标志事件 |
| `runClientGameTest -PtffClientTestSuite=mainline` | 通过 | 最终 `BUILD SUCCESSFUL`，约 2 分 1 秒；生存任务、标志记录、下界往返、要塞估算、最终夹具与存档重开全部通过 |
| 玩家文案禁止词扫描 + `git diff --check` | 通过 | `src/main`/`src/client` 未找到规定禁止说法，补丁无空白错误 |

## 阶段 5 验证中间问题与项目原有阻挡
- 本次改动引起的中间失败均已修复：旧 GameTest 断言、单一痕迹多人冲突、客户端旧进度夹具和安全落脚点。
- 纯 JUnit 运行时不含 Minecraft NBT 类；这是项目既有测试分层边界，因此 NBT 迁移测试在服务端 GameTest 中执行，不是未解决阻挡。
- 客户端运行仍会记录 Realms/账号无网鉴权、`Thumbs.db` 资源路径和缺失 `build/resources/client` 的既有警告；最终 `mainline` 成功，这些没有阻挡本次验证。
- 仓库仍没有 Git 提交，整个项目仍显示为未跟踪；按用户要求没有清理、提交或推送。
